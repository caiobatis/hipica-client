export const resizeOptions = {
  // unsharpAmount: The intensity of the sharpening mask. Higher values result in sharper images.
  unsharpAmount: 80,

  // unsharpRadius: The radius of the sharpening effect in pixels. Larger values affect more pixels around edges, enhancing perceived sharpness.
  unsharpRadius: 0.6,

  // unsharpThreshold: The threshold for applying the sharpening effect. Only color differences above this value will be enhanced.
  unsharpThreshold: 2,
}
